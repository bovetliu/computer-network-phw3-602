#include <stdio.h>
#include "Utility.hpp"
#include "CachedDocManager.hpp"

using namespace std;
int main(int argc, char **argv)
{
    
    char ipstr[INET_ADDRSTRLEN];        //INET6_ADDRSTRLEN for IPv6
    connection_info server_conn_info;
    
    Utility.initialize_server(argc, argv, server_conn_info);  // server_conn_info will be filled
    printf("initial sockfd: %d\n", server_conn_info.sockfd);
    
    char common_buf[2049];
    
	return 0;
}
